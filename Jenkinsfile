pipeline {
	agent any 
	stages {
		stage ("Compile") {
			steps {
		    	//gradlew compileJava
		    	echo "compileJava"
		    }
		}
		stage ("Unit test") {
			steps {
		    	//gradlew test
		    	echo "test"
		    }
		}
	}
}